/* @BEGIN_OF_SOURCE_CODE */
#include <iostream>
#include <string>
#include <vector>
#include <cmath>
using namespace std;
int main()
{
    vector<int> n;
    vector<int> d;
    vector<int>::iterator it;
    n.clear();
    d.clear();
    n.push_back(0);
    n.push_back(1);
    d.push_back(1);
    d.push_back(0);
    int t=100;
    while(t--)
    {
              
    for(it=n.begin();it<n.end();it++)
    {
    cout<<*n.end()<<endl;
                 n.insert(it,(*it)+(*(it+1)));
                 
                 }
//                 cout<<*it<<" "<<endl;
                 }
    for(it=n.begin();it<n.end();it++)
    cout<<*it<<" ";
    int i;
    cin>>i;
    return 0;
}
/* @END_OF_SOURCE_CODE */
